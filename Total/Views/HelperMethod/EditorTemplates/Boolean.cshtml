@model bool?

@if (ViewData.ModelMetadata.IsNullableValueType && Model == null)
{
    @:(true) (false) <b>(unset)</b>
}
else if (Model.Value)
{
    @:<b>(true)</b> (false) (unset)
}
else
{
    @:(true) <b>(false)</b> (unset)
}