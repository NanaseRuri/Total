@using Total.Models.HelperMethods
@model Total.Models.HelperMethods.Person

@{
    Layout = null;
    ViewBag.Title = "CreatePerson";
    Html.EnableClientValidation(false);
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>CreatePerson</title>
    <style>
        .ele-form {
            margin: 5px;
            float: left
        }

        label {
            display: inline-block;
            width: 80px;
        }
        input[type="submit"] {
            float: left;
            clear: both;
        }
    </style>
</head>
<body>
@*<form action="/HelperMethod/CreatePerson" method="post">
        <div class="ele-form">
            <label>PersonId: </label>
            @Html.TextBoxFor(m=>m.PersonId)
        </div>
        <div class="ele-form">
            <label>First: </label>
            @Html.TextBoxFor(m=>m.FirstName)
        </div>
        <div class="ele-form">
            <label>Role: </label>
            @Html.DropDownListFor(m=>m.Role,new SelectList(Enum.GetValues(typeof(Role))))
        </div>
        <input type="submit" value="submit"/>
        </form>
    *@

    @*@using (Html.BeginForm()) { }
    @using (Html.BeginForm("CreatePerson", "HelperMethod", FormMethod.Post, new { @class = "personClass" })) { }*@

@using (Html.BeginRouteForm("All", new { }, FormMethod.Post, new { date_formType = "person" }))
    {
        <div class="ele-form">
            @Html.EditorForModel()
            @*<label>First</label>
            <input name="FirstName"/>*@
        </div>

        <div class="ele-form">
            @Html.EditorFor(m => m.HomeAddress)
        </div>
        <input type="submit" value="submit" />
    
    }
</body>
</html>
